name: Manual Release Package

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Versão/tag a ser usada (ex: v1.2.0)'
        required: true
        default: 'v1.0.0'

permissions:
  contents: write   # permite criar/atualizar releases e enviar assets

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create tag if not exists
        run: |
          TAG="${{ github.event.inputs.version }}"
          echo "🔍 Verificando se a tag ${TAG} existe..."
          if ! git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "🔖 Criando tag ${TAG}..."
            git config user.name "github-actions"
            git config user.email "github-actions@github.com"
            git tag -a "$TAG" -m "Tag criada automaticamente para release"
            git push origin "$TAG"
          else
            echo "✅ Tag ${TAG} já existe."
          fi

      - name: Create release zip
        id: make_zip
        run: |
          TAG="${{ github.event.inputs.version }}"
          ZIPNAME="repo-${TAG}.zip"
          echo "📦 Criando pacote ${ZIPNAME}..."
          zip -r "${ZIPNAME}" . -x ".git/*" ".github/*"
          echo "ZIP=${ZIPNAME}" >> $GITHUB_OUTPUT
          ls -lh "${ZIPNAME}"

      - name: Create or update release and upload asset
        uses: softprops/action-gh-release@v1
        with:
          # Tag a ser usada (cria release se não existir, atualiza se existir)
          tag_name: ${{ github.event.inputs.version }}
          name: "Release ${{ github.event.inputs.version }}"
          body: "Release automático contendo o pacote completo do repositório (${{
            github.event.inputs.version }})."
          files: repo-${{ github.event.inputs.version }}.zip
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
